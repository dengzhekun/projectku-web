services:
  ai-service:
    environment:
      AI_EMBEDDING_REMOTE_URL: http://host.docker.internal:9001/embed
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "127.0.0.1:9000:9000"
