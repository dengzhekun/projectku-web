<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.mapper.UserWalletMapper">

    <select id="getByUserId" resultType="com.web.pojo.UserWallet">
        SELECT * FROM user_wallets WHERE user_id = #{userId}
    </select>

    <insert id="insertIfAbsent">
        INSERT IGNORE INTO user_wallets(user_id, balance, create_time, update_time)
        VALUES (#{userId}, #{balance}, NOW(), NOW())
    </insert>

    <update id="deductIfEnough">
        UPDATE user_wallets
        SET balance = balance - #{amount}, update_time = NOW()
        WHERE user_id = #{userId} AND balance >= #{amount}
    </update>

</mapper>
